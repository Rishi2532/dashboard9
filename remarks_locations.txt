3179:                                         <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-slate-100/80 dark:border-slate-800/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
3182:                                             const issues = esrIssuesMap?.get(esrKey) || [];
6096:                                                 <TableCell className="!px-3 !py-2 text-[12px] max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
6099:                                                     const issues = esrIssuesMap?.get(esrKey) || [];
7125:                                                 <TableCell className="!px-3 !py-2 text-[12px] max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
7128:                                                     const issues = esrIssuesMap?.get(esrKey) || [];
8635:                                       <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-orange-100/80 dark:border-orange-900/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
8638:                                           const issues = esrIssuesMap?.get(esrKey) || [];
8854:                                     <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-amber-100/80 dark:border-amber-900/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
8857:                                         const issues = esrIssuesMap?.get(esrKey) || [];
9064:                                       <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-orange-100/80 dark:border-orange-900/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
9067:                                           const issues = esrIssuesMap?.get(esrKey) || [];
9307:                                       <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-violet-100/80 dark:border-violet-900/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
9310:                                           const issues = esrIssuesMap?.get(esrKey) || [];
10479:                                           <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-emerald-100 dark:border-emerald-900 max-w-[150px] truncate" title={(villageIssuesMap?.get(`${village.scheme_id}-${village.village_name}`) || []).length > 0 ? (villageIssuesMap?.get(`${village.scheme_id}-${village.village_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
10482:                                               const issues = villageIssuesMap?.get(villageKey) || [];
11309:                                       <TableCell className="!px-3 !py-2.5 !text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-teal-100/80 dark:border-teal-900/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${sensor.scheme_id}-${sensor.village_name}-${sensor.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
11312:                                           const issues = esrIssuesMap?.get(esrKey) || [];
14391:                                         <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-purple-100/80 dark:border-purple-900/60 max-w-[150px] truncate" title={(esrIssuesMap?.get(`${item.scheme_id}-${item.village_name}-${item.esr_name}`) || []).length > 0 ? (esrIssuesMap?.get(`${item.scheme_id}-${item.village_name}-${item.esr_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
14394:                                             const issues = esrIssuesMap?.get(esrKey) || [];
14407:                                       <TableCell className="!px-4 !py-3 text-[12px] font-mono font-medium text-slate-700 dark:text-slate-300 border-r border-purple-100/80 dark:border-purple-900/60 max-w-[150px] truncate" title={(villageIssuesMap?.get(`${item.scheme_id}-${item.village_name}`) || []).length > 0 ? (villageIssuesMap?.get(`${item.scheme_id}-${item.village_name}`) || []).map((i: any) => i.reason).join(", ") : "-"}>
14410:                                           const issues = villageIssuesMap?.get(villageKey) || [];
15066:                             const issues = esrIssuesMap?.get(esrKey) || [];
15071:                               const issues = esrIssuesMap?.get(esrKey) || [];
